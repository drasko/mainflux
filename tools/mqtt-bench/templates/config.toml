[mqtt]
  [mqtt.broker]
  url = "tcp://localhost:1883"

  [mqtt.message]
  size = 100
  format = "text"
  qos = 0
  retain = false

  [mqtt.tls]
  mtls = false
  skiptlsver = true
  ca = "ca.crt"

[test]
pubs = 1
subs = 3
count = 10

[log]
quiet = false

[mainflux]
connections_file = "../provision/mfconn.toml"